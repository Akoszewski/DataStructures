#include "LinkedList.h"

/*template <typename T>
ostream& operator<<(ostream& out, const LinkedList<T>& list)
{
    Element<T>* temp = list.head;
    while(temp)
    {
        out << temp << endl;
        temp = temp->next;
    }
    return *this;
}*/